#Program to get and add two numbers together
#
#get first number.  note no checking for number type.
a = input("first number? ")
# get first number
b = input("second number? ")
#convert numbers to numbers and add.  
sum = float(a) + float(b)
print ("Sum is",sum)
